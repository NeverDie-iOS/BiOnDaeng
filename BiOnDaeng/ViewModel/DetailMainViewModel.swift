import SwiftUI
import AVKit

struct CctvView: View {
    var body: some View {
           VideoPlayer(player: AVPlayer(url: URL(string: //url
                    )!))
     }
}

#Preview {
    CctvView()
}
